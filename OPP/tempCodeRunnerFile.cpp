    // a.a =20;a.s ="con cac";
    // A b;b.a=50;b.s="con cac";
    
    // ofstream outFile("data.kBz",ios::app|ios::binary);
    // outFile.write(reinterpret_cast<char*>(&a),sizeof(a));
    // outFile.write(reinterpret_cast<char*>(&b),sizeof(b));
    // outFile.close();